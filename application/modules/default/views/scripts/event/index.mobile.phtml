<section id="events">
    <?php
        foreach ($this->events as $event)
        {
            echo $this->partial(
                'partial/event.phtml',
                array (
                    'item'  => $event
                )
            );
        }
    ?>
</section>
